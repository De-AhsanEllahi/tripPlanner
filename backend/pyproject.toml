[project]
name = "backend"
version = "0.1.0"
description = "ELD Trip Planner Backend"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "django>=5.0",
    "djangorestframework>=3.15",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.0.0"
]